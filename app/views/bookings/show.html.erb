<div class="d-flex flex-column justify-content-between align-items-center">
  <div class="d-flex justify-content-center">
    <h1 class="text-info">Flight info</h1>
  </div>
  <p>We appreciate your business and look forward to serving you.</p>
  <p>Please find below the details of your booking:</p>
  <p>Booking ID: #<%= @booking.id %></p>
  <p>Flight Number: <%= @booking.flight.id %></p>
  <p>Departure Airport: <%= @booking.flight.departure_airport.code %></p>
  <p>Arrival Airport: <%= @booking.flight.arrival_airport.code %></p>
  <p>Date of Flight: <%= @booking.flight.start_datetime.strftime("%d-%m-%Y") %></p>
</div>
<hr class="my-4">
<div class="d-flex justify-content-center">
  <h1 class="text-info">Your info</h1>
</div>
<div class="d-flex flex-column justify-content-between align-items-center">
  <% @booking.passengers.each do |passenger| %>
    <div class="d-flex justify-content-center">
      <p>Name: <%= passenger.name %></p>
    </div>
    <div class="d-flex justify-content-center">
      <p>Email: <%= passenger.email %></p>
    </div>
    <div>
      <hr class="my-4">
    </div>
  <% end %>
</div>
